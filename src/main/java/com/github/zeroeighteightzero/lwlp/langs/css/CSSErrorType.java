package com.github.zeroeighteightzero.lwlp.langs.css;

public enum CSSErrorType {
        SYNTAX_ERROR,
        INVALID_SELECTOR,
        UNEXPECTED_TOKEN,
        MISSING_SEMICOLON,
        INVALID_PROPERTY,
        INVALID_VALUE,
        UNCLOSED_BLOCK,
        DIVISION_BY_ZERO,
        UNKNOWN_ERROR
    }